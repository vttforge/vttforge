---
'@vttforge/vite-plugin': patch
---

First real release. Only a `0.0.0` name placeholder was on npm until now, and every scaffolded project depends on this package — so `vttforge init` produced a project that could not install.

No code change; the version bump is what publishes it.
