---
"@vttforge/core": patch
"@vttforge/cli": patch
"@vttforge/vite-plugin": patch
"@vttforge/styles": patch
"@vttforge/types": patch
---

Bring the package READMEs in line with what shipped. `core`, `styles`, `types` and `vite-plugin` still described themselves as v0.0.1 placeholders with "planned" features; `cli` did not mention `audit`.
