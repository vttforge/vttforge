---
"@vttforge/cli": patch
"@vttforge/core": patch
"@vttforge/dev-module": patch
"@vttforge/types": patch
"@vttforge/vite-plugin": patch
"@vttforge/testing": patch
"@vttforge/styles": patch
---

Rewrite the npm package descriptions to say what each package does today. `types` claimed full schema inference it does not have, `vite-plugin` claimed Handlebars HMR that lives in the dev loop, and `cli` did not mention `audit`.
