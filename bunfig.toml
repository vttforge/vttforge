[install]
# Mirrors pnpm's default (1440 min = 24h): no version installs until it
# has been published for 24h, except the packages listed below.
minimumReleaseAge = 86400
minimumReleaseAgeExcludes = ["knip", "@rolldown/plugin-babel"]
